# Versioning

Schemas use semantic versions. Additive optional fields are minor changes. Removing
fields, changing meanings, or tightening previously valid data is a major change.
Every release retains conformance fixtures and records the schema version on events.
